# ============================================================
# deploy-frontend.yml
# Trigger: push a main con cambios en front/ o shared/
# Hace: build Vite → deploy a Azure Static Web Apps
# ============================================================
name: Deploy Frontend

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'front/**'
      - 'shared/**'
      - '.github/workflows/deploy-frontend.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: front/package-lock.json

      # Instalar shared primero (monorepo dependency)
      - name: Install shared dependencies
        working-directory: shared
        run: npm install --legacy-peer-deps

      # Instalar front
      - name: Install frontend dependencies
        working-directory: front
        run: npm install --legacy-peer-deps

      # Build con variable de entorno apuntando al backend en Azure
      - name: Build frontend
        working-directory: front
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}   # https://inno-prod-api.azurecontainerapps.io/api
        run: npm run build

      # Deploy a Static Web Apps
      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: 'upload'
          app_location: 'front'
          output_location: 'dist'
          skip_app_build: true  # Ya construimos arriba con las env vars correctas
